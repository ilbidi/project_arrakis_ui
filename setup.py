# Setup.py: Setup aplplicazione
from setuptools import setup

setup(
    name='project_arrakis_ui',
    packages=['project_arrakis_ui'],
    include_package_data=True,
    install_requires=[
        'flask',
    ],
)
